server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_khu_vuc
    username: epigrid_admin
    password: 123456

# spring:
#   datasource:
#     url: ${DB_URL}
#     username: ${DB_USER}
#     password: ${DB_PASS}


  jpa:
      hibernate:
        ddl-auto: update
        naming:
          physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl  
      show-sql: true
      properties:
        hibernate:
          format_sql: true

geojson:
  path: geojson